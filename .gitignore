# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
src(backup)/

# 🧹 Working piles, not source (added 2026-08-21).
# `.scratch/` is one-off bench probes — evidence for a session, never a suite.
# `_to_delete/` is the quarantine the agent shell uses because it cannot unlink;
# empty it from a real terminal, do not commit it.
.scratch/
_to_delete/
